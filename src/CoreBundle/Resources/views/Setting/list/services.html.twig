<ul class="list-group">
    {% for entity in entities %}
    <li class="list-group-item">
        <h4 class="list-group-item-title">{{entity.name}}</h4>
        <div class="pull-right">
            <div class="btn btn-group">
                <small class="btn btn-primary btn-sm">{{entity.postes|length}} Postes associés</small>
                <span class="dropdown-toggle btn btn-primary btn-sm btn-addon" data-toggle="dropdown"><i class="fa fa-list"></i></span>
                <ul class="dropdown-menu">
                    {% for poste in entity.postes %}
                    <li><a>{{poste.name}}</a></li>
                    {% else %}
                    <li><a>Aucun poste</a></li>
                    {% endfor %}
                </ul>
            </div>
            <a href="{{path('core_setting_manage_update', {'id': entity.id, 'entity': 'service'})}}" class="btn btn-warning btn-sm"><i class="fa fa-edit"></i> Modifier</a>
            <a href="{{path('core_setting_delete', {'id': entity.id, 'type': 'service'})}}" data-confirm-message="Voulez-vous vraiment supprimer cette entité?" class="confirm btn btn-sm btn-danger"><i class="fa fa-remove"></i> Supprimer</a>
        </div>
        <div class="clearfix"></div>
    </li>
    {% endfor %}
</ul>