{% extends "core/base.html.twig" %} {% block main %}
<div class="col-lg-12">
    <br/>
    <div class="row">
        <div class="col-lg-12">
            <h2>{{entity|title}}</h2>
        </div>
        <div class="col-md-5">
            <h3>Formulaire d'ajout</h3>
            <form action="" method="post">
                {{form(form)}}
            </form>
        </div>
        <div class="col-md-7">
            <h3>Liste</h3> {% if entities|length > 0 %} {% set inclPage = "CoreBundle:Setting:list/"~entity~"s.html.twig" %} {% include inclPage with {'entities': entities} %} {% else %}
            <div class="well well-lg">
                <p class="text-center">Liste vide. Veuillez rentrer de nouvelles données.</p>
            </div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}